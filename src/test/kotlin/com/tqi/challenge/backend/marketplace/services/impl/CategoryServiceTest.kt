package com.tqi.challenge.backend.marketplace.services.impl

import io.mockk.junit5.MockKExtension
import org.junit.jupiter.api.*
import org.junit.jupiter.api.extension.ExtendWith


@ExtendWith(MockKExtension::class)
internal class CategoryServiceTest{

//    @MockK
//    lateinit var categoryRepository: CategoryRepository
//
//    @MockK
//    lateinit var categoryMapper:  CategoryMapper
//
//    @InjectMockKs
//    lateinit var categoryService: CategoryService
//
//
//    @Test
//    fun `should create category`() {
//
//        val categoryMock: Category = BuildCategory.buildCategory()
//        every { categoryRepository.save(any()) } returns categoryMock
//
//        val actual: CategoryRequestDTO = categoryService.createCategory(categoryMapper.map(categoryMock))
//        Assertions.assertThat(actual).isNotNull
//        Assertions.assertThat(actual).isSameAs(categoryMock)
//        verify(exactly = 1) { categoryRepository.save(categoryMock) }
//    }

}

